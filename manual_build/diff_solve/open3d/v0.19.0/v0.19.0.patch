diff --git a/3rdparty/embree/embree.cmake b/3rdparty/embree/embree.cmake
index 6d55198ec..fe456ab98 100644
--- a/3rdparty/embree/embree.cmake
+++ b/3rdparty/embree/embree.cmake
@@ -24,8 +24,9 @@ if(APPLE)
         set(ISA_LIBS embree_sse42)
         set(ISA_BUILD_BYPRODUCTS "<INSTALL_DIR>/${Open3D_INSTALL_LIB_DIR}/${CMAKE_STATIC_LIBRARY_PREFIX}embree_sse42${CMAKE_STATIC_LIBRARY_SUFFIX}" )
     endif()
-elseif(LINUX_AARCH64)
-    set(ISA_ARGS -DEMBREE_ISA_AVX=OFF
+elseif(LINUX_AARCH64 OR CMAKE_SYSTEM_PROCESSOR MATCHES "riscv")
+    set(ISA_ARGS -DEMBREE_MAX_ISA=NONE
+                 -DEMBREE_ISA_AVX=OFF
                  -DEMBREE_ISA_AVX2=OFF
                  -DEMBREE_ISA_AVX512=OFF
                  -DEMBREE_ISA_SSE2=OFF
diff --git a/3rdparty/find_dependencies.cmake b/3rdparty/find_dependencies.cmake
index 13f6501c8..c83511298 100644
--- a/3rdparty/find_dependencies.cmake
+++ b/3rdparty/find_dependencies.cmake
@@ -161,6 +161,7 @@ endfunction()
 # CMake arguments for configuring ExternalProjects. Use the second _hidden
 # version by default.
 set(ExternalProject_CMAKE_ARGS
+    -DCMAKE_POLICY_VERSION_MINIMUM=3.5
     -DCMAKE_C_COMPILER=${CMAKE_C_COMPILER}
     -DCMAKE_CXX_COMPILER=${CMAKE_CXX_COMPILER}
     -DCMAKE_CUDA_COMPILER=${CMAKE_CUDA_COMPILER}
@@ -1711,7 +1712,9 @@ else(OPEN3D_USE_ONEAPI_PACKAGES)
             DEPENDS      Open3D::3rdparty_tbb ext_mkl_include ext_mkl
         )
         if(UNIX)
-            target_compile_options(3rdparty_blas INTERFACE "$<$<COMPILE_LANGUAGE:CXX>:-m64>")
+            if(NOT LINUX_RISCV64)
+                target_compile_options(3rdparty_blas INTERFACE "$<$<COMPILE_LANGUAGE:CXX>:-m64>")
+            endif()
             target_link_libraries(3rdparty_blas INTERFACE Open3D::3rdparty_threads ${CMAKE_DL_LIBS})
         endif()
         target_compile_definitions(3rdparty_blas INTERFACE "$<$<COMPILE_LANGUAGE:CXX>:MKL_ILP64>")
@@ -1896,6 +1899,7 @@ if (BUILD_CUDA_MODULE)
 endif ()

 # embree
+if(NOT LINUX_RISCV64)
 if(USE_SYSTEM_EMBREE)
     open3d_find_package_3rdparty_library(3rdparty_embree
         PACKAGE embree
@@ -1916,6 +1920,7 @@ if(NOT USE_SYSTEM_EMBREE)
     )
 endif()
 list(APPEND Open3D_3RDPARTY_PRIVATE_TARGETS_FROM_CUSTOM Open3D::3rdparty_embree)
+endif()

 # WebRTC
 if(BUILD_WEBRTC)
diff --git a/3rdparty/uvatlas/uvatlas.cmake b/3rdparty/uvatlas/uvatlas.cmake
index 63642f80e..1eb44ac18 100644
--- a/3rdparty/uvatlas/uvatlas.cmake
+++ b/3rdparty/uvatlas/uvatlas.cmake
@@ -47,6 +47,7 @@ ExternalProject_Add(
     CMAKE_ARGS
         ${ExternalProject_CMAKE_ARGS_hidden}
         -DCMAKE_INSTALL_PREFIX=<INSTALL_DIR>
+        -DCMAKE_CXX_FLAGS=-D_XM_NO_INTRINSICS_
         -DBUILD_TOOLS=OFF
         -Ddirectx-headers_DIR=<INSTALL_DIR>
         -Ddirectxmath_DIR=<INSTALL_DIR>
diff --git a/CMakeLists.txt b/CMakeLists.txt
index 354125dc0..6af6f477d 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -14,6 +14,8 @@ if(UNIX AND NOT APPLE)
     )
     if(PROCESSOR_ARCH STREQUAL "aarch64")
         set(LINUX_AARCH64 TRUE)
+    elseif(PROCESSOR_ARCH MATCHES "^riscv")
+        set(LINUX_RISCV64 TRUE)
     endif()
 endif()
 if(APPLE)
diff --git a/cpp/open3d/core/SmallVector.h b/cpp/open3d/core/SmallVector.h
index 2f09c4ec2..177ea7fa6 100644
--- a/cpp/open3d/core/SmallVector.h
+++ b/cpp/open3d/core/SmallVector.h
@@ -28,6 +28,7 @@
 #include <algorithm>
 #include <cassert>
 #include <cstddef>
+#include <cstdint>
 #include <cstdlib>
 #include <cstring>
 #include <functional>
diff --git a/cpp/open3d/t/geometry/RaycastingScene.cpp b/cpp/open3d/t/geometry/RaycastingScene.cpp
index 0d4399f65..3f4687646 100644
--- a/cpp/open3d/t/geometry/RaycastingScene.cpp
+++ b/cpp/open3d/t/geometry/RaycastingScene.cpp
@@ -1,3 +1,7 @@
+#ifdef OPEN3D_RISCV64
+#include "open3d/t/geometry/RaycastingScene.h"
+namespace open3d { namespace t { namespace geometry { struct RaycastingScene::Impl {}; struct RaycastingScene::CPUImpl : RaycastingScene::Impl {}; RaycastingScene::RaycastingScene(int64_t, const core::Device&) : impl_(new Impl) {} RaycastingScene::~RaycastingScene() = default; uint32_t RaycastingScene::AddTriangles(const core::Tensor&, const core::Tensor&) { return INVALID_ID(); } uint32_t RaycastingScene::AddTriangles(const TriangleMesh&) { return INVALID_ID(); } std::unordered_map<std::string, core::Tensor> RaycastingScene::CastRays(const core::Tensor&, int) const { return {}; } core::Tensor RaycastingScene::TestOcclusions(const core::Tensor&, float, float, int) { return {}; } core::Tensor RaycastingScene::CountIntersections(const core::Tensor&, int) { return {}; } std::unordered_map<std::string, core::Tensor> RaycastingScene::ListIntersections(const core::Tensor&, int) { return {}; } std::unordered_map<std::string, core::Tensor> RaycastingScene::ComputeClosestPoints(const core::Tensor&, int) { return {}; } core::Tensor RaycastingScene::ComputeDistance(const core::Tensor&, int) { return {}; } core::Tensor RaycastingScene::ComputeSignedDistance(const core::Tensor&, int, int) { return {}; } core::Tensor RaycastingScene::ComputeOccupancy(const core::Tensor&, int, int) { return {}; } core::Tensor RaycastingScene::CreateRaysPinhole(const core::Tensor&, const core::Tensor&, int, int) { return {}; } core::Tensor RaycastingScene::CreateRaysPinhole(double, const core::Tensor&, const core::Tensor&, const core::Tensor&, int, int) { return {}; } uint32_t RaycastingScene::INVALID_ID() { return 0xffffffffu; } }}}
+#else
 // ----------------------------------------------------------------------------
 // -                        Open3D: www.open3d.org                            -
 // ----------------------------------------------------------------------------
@@ -1738,3 +1742,5 @@ struct formatter<RTCError> {
     }
 };
 }  // namespace fmt
+
+#endif
